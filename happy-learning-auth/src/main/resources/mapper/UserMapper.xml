<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happy.learning.zh.auth.mapper.UserMapper">
    <!-- 用户基础字段映射 -->
    <resultMap id="userResultMap" type="com.happy.learning.zh.auth.entity.User">
        <id column="user_id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="enabled" property="enabled"/>
        <collection property="roles" ofType="com.happy.learning.zh.auth.entity.Role"
                    resultMap="roleResultMap"/>
    </resultMap>

    <!-- 角色字段映射 -->
    <resultMap id="roleResultMap" type="com.happy.learning.zh.auth.entity.Role">
        <id column="role_id" property="id"/>
        <result column="name" property="name"/>
        <collection property="permissions" ofType="com.happy.learning.zh.auth.entity.Permission">
            <id column="perm_id" property="id"/>
            <result column="name" property="name"/>
            <result column="value" property="value"/>
        </collection>
    </resultMap>

    <!-- 核心查询 SQL -->
    <select id="findByUsernameWithRoles" resultMap="userResultMap">
        SELECT
        u.id AS user_id,
        u.username,
        u.phone ,
        u.password,
        u.enabled,
        u.email,
        r.id AS role_id,
        r.name ,
        r.description ,
        p.id AS perm_id,
        p.name ,
        p.value
        FROM t_user u
        LEFT JOIN t_user_role_relation ur ON u.id = ur.user_id
        LEFT JOIN t_user_role r ON ur.role_id = r.id
        LEFT JOIN t_user_role_permission_relation rp ON r.id = rp.role_id
        LEFT JOIN t_user_permission p ON rp.permission_id  = p.id
        WHERE u.username = #{username} or u.phone = #{username} or u.email = #{username}
    </select>
</mapper>
